import { Meta, Primary, Canvas, Controls } from "@storybook/blocks" 

import * as Story from "./TextBlock.stories"

<Meta of={Story} />

# Bloco de texto

Este é um componente de texto que pode ser usado para exibir textos com título em uma página.

### Primário
<Canvas of={Story.Primary} />

## Documentação Interativa:
<Primary of={Story} />
<Controls />
